{
  "name": "sistema-reservas-cancha-deploy",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "npm run build --prefix frontend",
    "start": "npm start --prefix backend"
  }
}
